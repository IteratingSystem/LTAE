# Worldloom 版本规范

Worldloom 和 Island 使用简单的三段式版本号：`主版本.次版本.修订版本`。Git 标签直接使用版本号，例如 `4.0.1`，不添加 `v` 前缀。

## 1. Worldloom 版本号

| 变化 | 何时增加 | 示例 |
| --- | --- | --- |
| 主版本 | 删除或修改公开 API、要求游戏项目大量迁移，或者存档无法兼容且没有迁移方案 | `4.2.3 -> 5.0.0` |
| 次版本 | 增加向后兼容的新功能、新组件或新系统 | `4.0.1 -> 4.1.0` |
| 修订版本 | 修复错误、优化性能、完善文档或调整构建流程，不破坏现有游戏代码 | `4.0.0 -> 4.0.1` |

版本升级时，低位归零。例如 `4.3.7` 增加次版本后是 `4.4.0`，不是 `4.4.7`。

不需要每次提交都修改版本号。可以连续提交开发内容，准备让 Island 拉取测试或正式使用时，再统一选择一个新版本发布。

## 2. 存档格式版本

引擎版本和存档格式版本是两件事：

- `worldloomVersion` 表示引擎发布版本。
- `saveFormatVersion` 表示 JSON 存档结构版本。

普通功能、渲染、音频或性能修改只增加引擎版本。只有存档 JSON 的结构发生不兼容变化时才增加 `saveFormatVersion`，并同时提供旧存档迁移逻辑。

## 3. Island 版本号

Island 的 `projectVersion` 独立于 Worldloom：

- 大型游戏版本或不兼容的存档变化增加主版本。
- 新地图、新玩法或一组完整内容增加次版本。
- 错误修复和小型调整增加修订版本。

Island 通过 `worldloomVersion` 固定依赖某个已发布的引擎版本。只有需要新的引擎功能或修复时才更新这个值；Worldloom 发布文档修订版时，Island 不必强制跟随。

## 4. 发布步骤

1. 完成代码和文档修改，并在 Worldloom 中运行测试。
2. 在 Island 中更新依赖并完成实际运行测试。
3. 根据变化范围修改 Worldloom 的 `gradle.properties` 中的 `worldloomVersion`。
4. 使用中文提交说明，例如 `发布 Worldloom 4.1.0`，并在提交正文中写明主要变化。
5. 推送 `main`。GitHub Actions 会创建同名标签和 Release。
6. 标签发布后不删除、不移动；发现问题时发布下一个修订版本。

版本号未变化时，GitHub Actions 会跳过发布，不会覆盖已有标签和 Release。

## 5. 提交说明

提交说明保持简短并描述结果即可，推荐使用以下开头：

- `新增`：增加功能。
- `修复`：修正错误。
- `优化`：性能或体验改进。
- `重构`：结构变化但功能保持一致。
- `完善`：文档、测试或构建流程调整。
- `发布`：只用于正式版本发布提交。
